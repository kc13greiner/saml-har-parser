# SAML-HAR Investigation Tool

## Start

### Angular

From the `angular-frontend` directory, run `npm install`. Then, run `ng serve`

### Node

From the `node-backend` directory, run `npm install`. Then, run `node server.js`
